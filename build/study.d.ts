import "reflect-metadata";
import { Camera } from "./Camera";
import { Sighting } from "./wildlife_sighting";
export declare class Study {
    readonly uuid: string;
    readonly study_name: string;
    private start_date;
    private end_date;
    private last_update;
    private description;
    private confidence_threshold;
    private batch_uuids;
    private cameras;
    private sightings;
    constructor(uuid: string, study_name: string, end_date: number | null, description: string, confidence_threshold: number);
    get_start_date_as_date(): Date;
    get_end_date_as_date(): Date | null;
    get_last_updated_as_date(): Date;
    get_cameras(): Camera[];
    get_num_cameras(): Number;
    add_camera_station(camera: Camera): void;
    remove_camera_station(camera: Camera): void;
    get_camera_by_uuid(uuid: string): Camera | null;
    get_camera_by_camera_id(camera_id: string): Camera | null;
    get_num_sightings(): number;
    add_sighting(sighting: Sighting): void;
    remove_sighting(sighting: Sighting): void;
    get_sighting_by_image_id(image_id: string): Sighting | null;
    verify_data_batch_uuid(batch_uuid: string): boolean;
    add_data_batch_uuid(batch_uuid: string): void;
    remove_data_batch_id(batch_uuid: string): void;
}
