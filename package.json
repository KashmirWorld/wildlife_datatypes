{
  "name": "wildlife_datatypes",
  "version": "1.0.0",
  "description": "Data Types for mobile and desktop wildlife app",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc --build --clean && tsc --build --verbose",
    "prepack": "tsc --build --clean && tsc --build --verbose",
    "test": "mocha --exit -r ts-node/register tests/*.*"
  },
  "keywords": [],
  "author": "KWF",
  "license": "ISC",
  "dependencies": {
    "class-transformer": "^0.5.1",
    "mocha": "^10.2.0",
    "reflect-metadata": "^0.1.13",
    "typescript": "^5.1.3"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/uuid": "^9.0.2",
    "ts-node": "^10.9.1"
  }
}
